export default class particleCategoryModel {
   constructor(title, color, items) {
        this.title = title;
        this.color = color;
        this.items = items; // array of ParticleContent
    }
}